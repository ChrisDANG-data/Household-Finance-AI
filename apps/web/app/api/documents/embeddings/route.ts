import { notImplemented } from "@/utils/api-response";

/** POST — Document Intelligence: index document chunks (vectors) */
export async function POST() {
  return notImplemented("document-intelligence/embeddings");
}
