import { notImplemented } from "@/utils/api-response";

/** POST — Forecast Simulation: deterministic what-if scenario */
export async function POST() {
  return notImplemented("forecast-simulation/scenarios");
}
